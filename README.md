# Students-Roll-number-and-Name-Table
A dynamic Student Registration System built with HTML, CSS, and JavaScript. It uses DOM manipulation to insert student details into a table instantly without reloading the page.
# 🎓 Student Registration Data Table

A simple and interactive web application that allows users to input student details and adds them dynamically to a table using JavaScript DOM manipulation.

## 🚀 Features

- **Dynamic Data Insertion:** Adds new rows to the table instantly without refreshing the page.
- **Form Validation:** Ensures that Roll Number, Name, and Gender fields are not empty before submitting.
- **Auto-Incrementing Serial Number:** Automatically adds a serial number (Sr. No) for every new entry.
- **Input Reset:** Automatically clears the input fields after a successful entry for better user experience.
- **Responsive Design:** Styled with CSS using Flexbox to ensure the form looks good on different screen sizes.

## 🛠️ Technologies Used

- **HTML5:** For the structure of the form and table.
- **CSS3:** For styling the container, buttons, and table layout.
- **JavaScript (ES6):** For DOM manipulation, event handling, and logic.

## 📂 Project Structure

```text
├── index.html   # Contains HTML, CSS, and JavaScript logic
└── README.md    # Project documentation
